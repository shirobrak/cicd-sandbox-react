name: "unit-test"

on:
  pull_request:

jobs:
  jest:
    name: "jest"
    runs-on: "ubuntu-latest"
    steps:
      - uses: actions/checkout@v2
      - name: yarn install
        run: yarn install
      - name: yarn jest
        run: yarn jest
